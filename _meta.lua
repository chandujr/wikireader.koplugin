local _ = require("gettext")
return {
    fullname = _("Wikipedia Reader"),
    description = _([[Search Wikipedia (or open today's featured article) and read the result immediately as a formatted book, without permanently saving it to your library.]]),
}
